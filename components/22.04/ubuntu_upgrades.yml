name: UpdateUbuntu
description: Use AWS's build upgrade TOE for updating the system
schemaVersion: 1.0
component_version: 0.0.4

phases:
  - name: build
    steps:
      - name: UpdateUbuntu
        action: UpdateOS
