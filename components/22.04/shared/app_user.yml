name: AddAppUser
description: Creates a app user
schemaVersion: 1.0
component_version: 0.0.1

phases:
  - name: build
    steps:
      - name: AddUser
        action: ExecuteBash
        inputs:
          commands:
            - sudo apt install -y binutils build-essential curl git unzip
