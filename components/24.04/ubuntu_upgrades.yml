name: UpdateUbuntu
description: Use AWS's build upgrade TOE for updating the system
schemaVersion: 1.0
component_version: 0.0.1

phases:
  - name: build
    steps:
      - name: UpdateUbuntu
        action: UpdateOS
